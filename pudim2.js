const minhaFuncao = (array) => array.map((element) => element * 2);

// Exemplo de uso
const meuArray = [1, 2, 3, 4];
const resultado = minhaFuncao(meuArray);
console.log(resultado); // Saída esperada: [2, 4, 6, 8]
